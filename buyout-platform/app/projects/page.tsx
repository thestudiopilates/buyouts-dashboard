import { ModulePlaceholder } from "@/components/module-placeholder";

export default function ProjectsPage() {
  return (
    <ModulePlaceholder
      activeHref="/projects"
      eyebrow="Projects module"
      title="Projects"
      description="This will become the cross-team project and initiative workspace after the buyouts workflow is stabilized in the new platform."
    />
  );
}
